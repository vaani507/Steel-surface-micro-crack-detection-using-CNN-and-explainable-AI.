// User-overridable Next.js config merged by next.config.mjs.
// Keep this file present so local dev does not fail on missing import.

/** @type {import('next').NextConfig} */
const userConfig = {}

export default userConfig
